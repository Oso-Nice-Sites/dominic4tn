// Drizzle schema for D1 (SQLite). Tables are added here once the data model
// is settled — intake forms (volunteer, yard sign, contact) will store PII
// as encrypted columns; see the build brief for the field list.
export {};
